# Tutorial: 국내 플랫폼 개발자 셋업 (Cafe24 · 네이버 커머스 · 쿠팡 · 네이버 검색광고 · 카카오모먼트)

각 플랫폼에서 **우리(개발사)가 키/앱을 발급받는 절차**. (데이터 호출은 [cafe24.md](./cafe24.md), [naver-commerce.md](./naver-commerce.md), [coupang.md](./coupang.md), [naver-search-ad.md](./naver-search-ad.md), [kakao-moment.md](./kakao-moment.md))

> 국내 개발자센터는 대부분 로그인+JS 렌더링이라, 아래는 공식 문서·가이드 교차확인 기준. 화면 라벨은 버전마다 바뀔 수 있음.

---

## 1. Cafe24 (developers.cafe24.com)

1. **가입:** 카페24 계정으로 로그인 → Apps → **개발사(자) 등록**. 무료.
2. **앱 생성:** 개발가이드 → 앱 생성 → 종류(일반/할인/배송/PG) 선택. (앱명 중복·미개설 몰은 불가)
3. **키 발급:** 앱 → 개발정보 → **인증정보** 탭 → **App Key**(=Client ID), **App secret key**(=Client Secret).
4. **Redirect URI / scope:** 필수정보 등록에서 App URL + **Redirect URI**(인증코드 유효 1분) + **scope**(`mall.read_order` 등 콤마 구분) 설정.
5. **API 버전:** 날짜 버전(예 `2026-03-01`), 헤더 `X-Cafe24-Api-Version`. 인증정보의 버전 관리에서 설정(유효 1년).
6. **배포 vs 자체사용:**
   - **자체사용/프라이빗:** 개발정보 → "테스트 실행"에 내 쇼핑몰 ID 입력 → 심사 없이 설치·사용.
   - **앱스토어 배포:** 출시가이드 → 판매정보 등록 → **보안 심사**(시스템·정보보안) 승인.

## 2. 네이버 커머스 API센터 (apicenter.commerce.naver.com)

1. **가입:** 스마트스토어 **통합매니저** 계정만 개발업체 계정 생성 가능. 무료.
2. **앱 등록:** 내 스토어 애플리케이션 → 애플리케이션 등록 → ① 앱 정보 → ② **API 호출 IP 입력(최대 3개)** → ③ **API 그룹 5종 모두 추가** → ④ 등록.
3. **키:** 등록 완료 시 **Application ID + Secret** 발급. (스토어당 앱 최대 1개)
4. **인증:** OAuth `client_credentials` + bcrypt 전자서명, 등록 IP에서만 호출. (코드는 naver-commerce.md)
5. **유형:**
   - **내 스토어 앱:** 통합매니저 본인 스토어용 — 위 절차로 즉시.
   - **커머스솔루션/API대행사:** 여러 판매자 서비스용 — 별도 신청 유형(세부 양식 로그인 영역, 미확인). SELLER 토큰엔 Quota limit 추가.
6. ⚠️ 통합매니저 변경 시 기존 앱 삭제 → 재생성 필요.

## 3. 쿠팡 WING OpenAPI (wing.coupang.com)

1. **가입:** WING 판매자 등록 + **사업자 인증 필수**(미인증 시 키 발급 불가). 무료. 테스트 환경 없음.
2. **키 발급:** WING 로그인 → 우상단 ID → 판매자정보/추가판매정보 → **API Key 발급** → OPEN API 선택 → 약관 동의.
3. **연동 방식 선택:**
   - **연동업체:** 목록에서 1개사만 선택.
   - **자체개발:** 업체명 + URL + IP 직접 입력.
4. **IP:** 서버 IP **최대 10개** 등록(연동정보 수정 주 10회).
5. **키:** Access Key / Secret Key 화면 확인. **유효기간 180일**(만료 14일 전부터 재발급 버튼 활성, 사전 메일 안내).
6. ⚠️ 발급은 즉시지만 실제 access 권한은 24시간+ 걸릴 수 있음.

## 4. 네이버 검색광고 API (searchad.naver.com)

1. **가입:** 검색광고 광고주 계정. 무료.
2. **키 발급:** 로그인 → 광고플랫폼 → 상단 **[도구] → [API 사용 관리]** → **API 서비스 신청** → 약관 동의 → 저장.
3. **키:** 자동 발급 — **CUSTOMER_ID + 액세스라이선스 + 비밀키**.
4. **인증:** HMAC 서명(Redirect URI·IP 화이트리스트 없음). 심사 없이 약관 동의 즉시. (코드는 naver-search-ad.md)

## 5. 카카오모먼트 (developers.kakao.com + 카카오비즈니스)

> ⚠️ 가장 제약 강함 — **카카오 광고 공식대행사 / 협의된 광고주만** 권한 부여. 일반 개발자 자유 발급 불가.

1. **앱 생성:** 카카오디벨로퍼스에서 앱 생성 → REST API 키 발급.
2. **비즈 앱 전환:** 카카오 로그인 'ON', 동의항목 '사용 안 함' → **비즈니스 인증**(앱 소유자 본인인증).
3. **권한 신청:** 앱 관리 → 추가 기능 신청 → **Moment 오픈API 사용 권한 신청**.
   - 광고주: 담당 카카오 마케터 경유.
   - 공식대행사: 카카오광고 통합 에이전시 문의게시판 경유.
4. **승인:** 신청 후 3~5영업일 내 권한 부여(메일 알림). 호출은 **비즈니스 토큰** 사용. (코드는 kakao-moment.md)

---

## 핵심 비교

| 플랫폼 | 키 발급 위치 | redirect/IP | 승인 | 키 만료 |
| --- | --- | --- | --- | --- |
| Cafe24 | 앱 > 개발정보 > 인증정보 | Redirect URI(코드 1분) | 자체사용 불필요 / 앱스토어 심사 | 토큰 기반, 버전 1년 |
| 네이버 커머스 | 내 스토어 애플리케이션 | 호출 IP 최대 3개 | 통합매니저 즉시 / 대행사 별도 | — |
| 쿠팡 WING | 판매자정보 > API Key | IP 최대 10개 | 사업자인증 필수 | **180일** |
| 네이버 검색광고 | 도구 > API 사용 관리 | 없음(서명) | 약관 동의 즉시 | — |
| 카카오모먼트 | 디벨로퍼스 + 추가기능 신청 | 카카오 로그인 Redirect | **대행사/협의 한정**, 3~5영업일 | — |
